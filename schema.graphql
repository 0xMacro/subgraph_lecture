# The objects we want to query for



type Gravatar @entity {
  id: ID!
  owner: Bytes!
  displayName: String!
  imageUrl: String!
}

type GravatarUpdated @entity {
  id: ID!
  owner: Bytes!
  block: Bytes!
  txId: Bytes!
}
